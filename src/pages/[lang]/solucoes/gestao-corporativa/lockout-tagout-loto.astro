---
// src/pages/[lang]/solucoes/gestao-corporativa/lockout-tagout-loto.astro
export const prerender = true;
import ServicePage from '../../../../components/ServicePage.astro';
import { getPagePayload } from '../../../../i18n/payloadLoader';

import { LOCALES } from '../../../../i18n/config';

export function getStaticPaths() {
  return LOCALES.map(lang => ({ params: { lang } }));
}

const currentLang = Astro.currentLocale || 'pt';
const pagePayload = getPagePayload('lockout-tagout-loto', currentLang);
---

<ServicePage
  page={pagePayload as any}
  lang={currentLang}
/>
